# Watchdog Report Triage — 2026-07-29

## Item 1 — stale-path evidence: DIAGNOSED, no read-path bug (deploy-timing)

**Claim:** the 7:01 report showed learning-stall as CRITICAL/919h/"check crons" and social as chronic-high, contradicting yesterday's deployed fixes.

**Finding — a 6-minute deploy-timing miss, not an architecture/read-path bug:**
- The daily report is generated by **`system-watchdog` itself** (not a separate email generator). Cron: `system-watchdog-daily @ 0 13 * * *` = **13:00 UTC = 7:00 MDT**, once daily.
- The email narrative is built from the **annotated `findings` array**: `analysis.findings = [...findings, ...analysis.findings]` (index.ts ~4339). My code findings (with my severities) are merged in and their severity/plainEnglish are **preserved, not AI-re-rated**. So there is no "raw findings upstream of annotations" path to fix.
- The 7:01 report showed **919h ≈ `agent_beliefs.last_updated_at` (922h)** — the OLD anchor. My re-anchor uses `created_at` (**1516h**). So the report ran the **pre-fix version**. The current deploy **v177 (13:07 UTC) landed 6–7 minutes AFTER the 13:00 report ran.**
- **Verified deployed v177 contains all three fixes:** re-anchor to `created_at` (PRESENT), freeze-aware `frozen ? 'medium' : 'critical'` (PRESENT), `KNOWN_DEFERRED_SOCIAL` (PRESENT); old `last_updated_at` anchor (ABSENT). `has_learning_freeze()` returns **true** in prod.
- The social **"KNOWN LIMITATION (deferred by ruling)" finding is already `low` and active** in `platform_findings` (the annotation works).

**Acceptance test (tomorrow 7:00 / 13:00 UTC, v177) will show:** learning = **medium** "belief writes frozen (INC-LEARN-CONTAM) — no cron action" (contained-and-known); social = **low** "known limitation." No code change required. (Not triggered manually to verify because a report already went out today — the 20h email-dedup + the still-critical severity regression would fire a duplicate email; the logic is deterministic given `has_learning_freeze()=true`.)

## Item 2 — severity regression: Phase 1 EVIDENCE (HELD for ruling)

72.7% high/crit over 7d (384 signals) vs the ~18% ceiling and June's 4.5%.

**(a) Where the inflation is concentrated:**
- **`monitor-rss-sources`: 270 signals (70% of all volume), 86.3% high/crit.** This is the recovered CBC/news RSS feed and it dominates.
- Other sources are fine: monitor-naad-alerts 0%, monitor-news-google 0%, monitor-wildfires 54.5%, unknown-legacy 49.3%.
- By category, **100% high/crit**: cybersecurity, environmental, general, protest; **96.3%** regulatory; **92.2%** civil_emergency; **87.5%** health_concern; **80.2%** operational. (Lower: active_threat 39%, social_sentiment 50%, weather 0%.)

**(b) Is the June recalibration active or bypassed?** The recalibration mechanism is the **analyst-feedback few-shot injection** in `ingest-signal` (L862–906: "ANALYST CALIBRATION EXAMPLES" — reads `feedback_events` with `feedback ∈ {irrelevant, wrong_severity, confirmed}` + notes, tenant-scoped, limit 8). It is **still deployed but STARVED:**
- **`wrong_severity` examples: 0 — ever.** The calibration has never had a single severity-correction example, so it structurally cannot teach severity dampening (only irrelevance/confirmation).
- Analyst feedback **stopped 2026-07-15** (last usable feedback; 14d stale). Only 7 usable examples in the last 30d.
- So the feedback-driven calibration path is not correcting severity — the classifier reverts to its default, which rates news coverage by the severity of the *event described*, not client-actionability.

**(c) Prime suspect — confirmed.** Recovered RSS/news volume is the driver: 86% of it rates high/crit, and it's 70% of all signal volume. News *about* severe events (cyberattacks, wildfires, regulatory actions) is classified high/crit by default because the classifier grades the world-event severity, not "is this an actionable high-severity signal for PECL." Wildfire coverage specifically is 54.5% (not the worst) — the biggest offenders are cyber/environmental/regulatory/civil_emergency news at 92–100%.

**HELD for ruling.** Root shape: a starved calibration loop (no `wrong_severity` feedback, feedback stopped 07-15) + a recovered high-volume news firehose whose default classification is world-event-severity, not client-actionability. Candidate fixes (Phase 2): re-seed/ generate `wrong_severity` calibration examples; add a news-vs-actionable severity rule (grade the client impact, not the headline); or cap news-source severity absent a client pathway (ties to WO-HAZARD/pathway doctrine).

**Ledger:** the watchdog caught a regression of a previously-fixed defect (the June severity recalibration to 4.5%) — that is the system working. Recorded.

## Item 3 — fleet-dormant (8/42): DONE

Annotated the `≥80% dormant` finding in `system-watchdog` to **`low` / KNOWN STRATEGIC**, referencing the honest-capability ledger (alongside confidence-sparsity + learning-loop). Now reports as tracked-not-actionable-today, not a fresh high-severity defect. Deployed.

## Item 4 — orphaned-signals auto-fix: accepted, no action.
