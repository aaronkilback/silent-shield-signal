-- This migration was applied directly to the remote database without
-- being committed as a file. This placeholder exists only to satisfy the
-- supabase CLI's drift check so future migrations can be pushed.
-- The schema changes (whatever they were) are already live in production.
SELECT 1;
